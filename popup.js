// // popup.js
// chrome.runtime.onMessage.addListener(function(request, sender, sendResponse) {
    //     if (request.text) {
        //       document.getElementById('highlightedText').textContent = request.text;
//       // Additional code to show/hide the popup or perform other actions
//     }
//   });

// popup.js
// chrome.runtime.onMessage.addListener(function(request, sender, sendResponse) {
//     document.body.style.backgroundColor = 'red'
//     if (request.text) {
//       console.log('Received message:', request.text);
//       // Additional code to handle the received message
//     }
//   });
  